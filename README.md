# Professional Portfolio - Sachin Kumar

A premium, single-page personal portfolio website built with semantic HTML5, modern CSS3 (Glassmorphism, Neon accents), and Vanilla JavaScript. Designed for high performance and easy deployment on GitHub Pages.

## 🚀 Features
- **Premium UI/UX**: Dark mode aesthetic with neon glows and glassmorphism effects.
- **Responsive Design**: Fully optimized for Desktop, Tablet, and Mobile.
- **Smooth Animations**: Scroll-triggered reveal animations and interactive elements.
- **Dynamic Content**: Auto-updating copyright year, mobile menu, and smooth scrolling.
- **Top Efficiency**: Zero external framework dependencies (React, Vue, etc.) for maximum speed and simplicity.

## 🛠️ Tech Stack
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Icons**: FontAwesome 6.4
- **Fonts**: 'Outfit' (Headings) & 'Inter' (Body) from Google Fonts

## 📂 Project Structure
```
/
├── index.html       # Main HTML file
├── style.css        # Premium Global Styles & Animations
├── script.js        # UI Logic (Scroll, Menu, Animations)
└── assets/          # Directory for images (add your project screenshots here)
```

## 📦 How to Deploy to GitHub Pages

1.  **Initialize Git**:
    ```bash
    git init
    git add .
    git commit -m "Initial commit - Premium Portfolio"
    ```

2.  **Push to GitHub**:
    - Create a new repository on GitHub (e.g., `my-portfolio`).
    - Connect and push:
    ```bash
    git remote add origin https://github.com/yourusername/my-portfolio.git
    git branch -M main
    git push -u origin main
    ```

3.  **Enable GitHub Pages**:
    - Go to Repository **Settings** > **Pages**.
    - under **Source**, select `main` branch and `/ (root)` folder.
    - Click **Save**.
    - Your portfolio will be live at `https://yourusername.github.io/my-portfolio/` in a few minutes!

## 🔧 Local Development
Simply open `index.html` in your web browser. No local server required (though `Live Server` extension in VS Code is recommended for auto-reloads).
